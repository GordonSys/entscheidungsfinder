build:
  stage: build
  image: ubuntu:latest
  script:
    - "apt-get update && apt-get upgrade -y && apt-get install -y build-essential cmake gcc g++"
    - "cmake ."
    - "make"
  artifacts:
    paths:
      - entscheidungsfinder